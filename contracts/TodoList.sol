pragma solidity ^0.5.0;

contract TodoList {
  //state variable - the actual state of the smart contract on the blockchain
  //similar to class variables
  uint public taskCount = 0;

  //abi = abstract binary interface
}
